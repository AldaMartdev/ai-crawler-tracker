{
  "name": "ai-crawler-tracker",
  "private": true,
  "version": "0.0.0",
  "description": "Open source analytics to discover which AI agents are reading your site",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
